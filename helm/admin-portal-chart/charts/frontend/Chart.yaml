apiVersion: v2
name: frontend
description: A seperate chart for the Angular app nested inside an umbrella chart, i.e. admin-portal 
version: 1.0.0 # THe first version of the frontend chart
appVersion: "1.0" # Codebase did not change for the frontend since te last commit
type: application