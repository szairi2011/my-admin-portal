(function (window) {
  window['env'] = window['env'] || {};

  // Environment variables
  // For a Dev environment, the App properties values are currently picked up from environment.ts file
})(this);
